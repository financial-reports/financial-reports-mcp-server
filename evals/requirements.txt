# Eval harness deps (separate from the server runtime).
mcp>=1.2.0            # official MCP SDK — Streamable HTTP client
anthropic>=0.40.0    # Claude adapter + judge
openai>=1.40.0       # OpenAI-compatible adapter (DeepSeek via Ollama Cloud)
PyYAML>=6.0          # task files
boto3>=1.34.0        # cognito_token.py (token path A); optional otherwise
pytest>=8.0          # unit tests for the pure scoring/grading layer
